# 📤 คู่มือการใช้งานฟีเจอร์อัพโหลดข้อมูลพืช

## ✨ ฟีเจอร์ที่เพิ่มเข้ามา

### 1. **Composable: usePlantData** (`src/composables/usePlantData.js`)

เพิ่มฟังก์ชันสำหรับจัดการข้อมูลพืช:

- ✅ `fetchPlantData(plantId)` - ดึงข้อมูลพืชจาก API
- ✅ `uploadPlantData(plantId, data)` - อัพโหลดข้อมูลพืชใหม่ (POST)
- ✅ `updatePlantData(plantId, data)` - อัพเดทข้อมูลพืชที่มีอยู่ (PUT)

**State ที่มี:**
- `plantData` - ข้อมูลพืช
- `loading` - สถานะการโหลดข้อมูล
- `uploading` - สถานะการอัพโหลด
- `uploadSuccess` - สถานะความสำเร็จในการอัพโหลด
- `error` - ข้อความ error (ถ้ามี)

### 2. **Component: PlantDataUploadForm** (`src/components/PlantDataUploadForm.vue`)

ฟอร์มสำหรับอัพโหลดและแก้ไขข้อมูลพืช พร้อมฟีเจอร์:

- 🎨 UI ที่สวยงามและใช้งานง่าย
- 📱 Responsive design (ใช้งานได้ทั้ง Desktop และ Mobile)
- ✨ Animation เมื่อเปิด/ปิดฟอร์ม
- ✅ แสดงสถานะ Loading, Success, และ Error
- 🔄 Auto-reset ฟอร์มหลังอัพโหลดสำเร็จ

**ฟิลด์ในฟอร์ม:**
- รหัสพืช (Plant ID) - **จำเป็น**
- ชื่อทั่วไป (ภาษาไทย)
- ชื่อวิทยาศาสตร์
- วงศ์ (Family)
- ถิ่นกำเนิด
- คำอธิบาย
- วิธีการดูแล
- มีพิษหรือไม่ (Checkbox)

## 🚀 วิธีใช้งาน

### การอัพโหลดข้อมูลพืชใหม่

1. เปิดหน้าเว็บไซต์
2. คลิกปุ่ม **"อัพโหลด/แก้ไขข้อมูลพืช"**
3. กรอกข้อมูลในฟอร์ม:
   - **รหัสพืช**: เช่น `monstera-deliciosa` (ต้องกรอก)
   - กรอกข้อมูลอื่นๆ ตามต้องการ
4. คลิกปุ่ม **"อัพโหลดข้อมูล"**
5. รอจนกว่าจะแสดงข้อความ "อัพโหลดสำเร็จ!"
6. ฟอร์มจะปิดอัตโนมัติหลังจาก 2 วินาที

### การอัพเดทข้อมูลพืชที่มีอยู่

1. เปิดฟอร์มเช่นเดียวกับการอัพโหลด
2. กรอก **รหัสพืช** ที่ต้องการแก้ไข
3. กรอกข้อมูลใหม่ที่ต้องการเปลี่ยน
4. คลิกปุ่ม **"อัพโหลดข้อมูล"** หรือ **"อัพเดทข้อมูล"**

## 🔧 การใช้งาน Composable แบบ Advanced

### ตัวอย่างการใช้ใน Component อื่น

```vue
<script setup>
import usePlantData from '@/composables/usePlantData'
import { onMounted } from 'vue'

const { 
  plantData, 
  loading, 
  error, 
  uploading,
  uploadSuccess,
  fetchPlantData,
  uploadPlantData,
  updatePlantData 
} = usePlantData()

// ดึงข้อมูลพืช
onMounted(() => {
  fetchPlantData('monstera-deliciosa')
})

// อัพโหลดข้อมูลใหม่
const handleUpload = async () => {
  const newData = {
    commonName: 'มอนสเตอร่า',
    scientificName: 'Monstera deliciosa',
    family: 'Araceae',
    origin: 'เม็กซิโก',
    description: 'พืชประดับยอดนิยม...',
    careInstructions: 'รดน้ำปานกลาง...',
    isToxic: true
  }
  
  try {
    await uploadPlantData('monstera-deliciosa', newData)
    console.log('อัพโหลดสำเร็จ!')
  } catch (err) {
    console.error('อัพโหลดล้มเหลว:', err)
  }
}

// อัพเดทข้อมูล
const handleUpdate = async () => {
  const updatedData = {
    description: 'คำอธิบายใหม่...'
  }
  
  try {
    await updatePlantData('monstera-deliciosa', updatedData)
    console.log('อัพเดทสำเร็จ!')
  } catch (err) {
    console.error('อัพเดทล้มเหลว:', err)
  }
}
</script>

<template>
  <div>
    <div v-if="loading">กำลังโหลด...</div>
    <div v-else-if="error">{{ error }}</div>
    <div v-else>
      <h2>{{ plantData?.commonName }}</h2>
      <button @click="handleUpload" :disabled="uploading">
        อัพโหลดข้อมูล
      </button>
      <button @click="handleUpdate" :disabled="uploading">
        อัพเดทข้อมูล
      </button>
      <div v-if="uploadSuccess">✅ ดำเนินการสำเร็จ!</div>
    </div>
  </div>
</template>
```

## 📡 API Endpoints ที่ต้องมีใน Backend

สำหรับให้ฟีเจอร์นี้ทำงานได้ Backend ของคุณต้องมี endpoints เหล่านี้:

### 1. GET - ดึงข้อมูลพืช
```
GET /api/data-info/:plantId
```

**Response Example:**
```json
{
  "plantId": "monstera-deliciosa",
  "commonName": "มอนสเตอร่า เดลิซิโอซ่า",
  "scientificName": "Monstera deliciosa",
  "family": "Araceae",
  "origin": "เม็กซิโก",
  "description": "พืชประดับยอดนิยม...",
  "careInstructions": "รดน้ำปานกลาง...",
  "isToxic": true
}
```

### 2. POST - สร้างข้อมูลพืชใหม่
```
POST /api/data-info/:plantId
Content-Type: application/json
```

**Request Body:**
```json
{
  "commonName": "มอนสเตอร่า เดลิซิโอซ่า",
  "scientificName": "Monstera deliciosa",
  "family": "Araceae",
  "origin": "เม็กซิโก",
  "description": "พืชประดับยอดนิยม...",
  "careInstructions": "รดน้ำปานกลาง...",
  "isToxic": true
}
```

### 3. PUT - อัพเดทข้อมูลพืช
```
PUT /api/data-info/:plantId
Content-Type: application/json
```

**Request Body:** (เหมือน POST)

## ⚙️ Configuration

ตรวจสอบให้แน่ใจว่า `vite.config.js` มีการตั้งค่า proxy:

```javascript
export default defineConfig({
  // ... other config
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:3000',
        changeOrigin: true,
      }
    }
  }
})
```

## 🎯 ตัวอย่าง Backend (Node.js/Express)

```javascript
const express = require('express')
const app = express()

app.use(express.json())

// GET - ดึงข้อมูล
app.get('/api/data-info/:plantId', (req, res) => {
  const { plantId } = req.params
  // ดึงข้อมูลจาก database
  res.json({
    plantId,
    commonName: 'มอนสเตอร่า',
    scientificName: 'Monstera deliciosa',
    // ... ข้อมูลอื่นๆ
  })
})

// POST - สร้างข้อมูลใหม่
app.post('/api/data-info/:plantId', (req, res) => {
  const { plantId } = req.params
  const data = req.body
  // บันทึกข้อมูลลง database
  res.json({ success: true, ...data })
})

// PUT - อัพเดทข้อมูล
app.put('/api/data-info/:plantId', (req, res) => {
  const { plantId } = req.params
  const data = req.body
  // อัพเดทข้อมูลใน database
  res.json({ success: true, ...data })
})

app.listen(3000, () => {
  console.log('Server running on http://localhost:3000')
})
```

## 🐛 Troubleshooting

### ปัญหา: CORS Error
**แก้ไข:** ตรวจสอบว่า Vite proxy configuration ถูกต้อง และ restart dev server

### ปัญหา: 404 Not Found
**แก้ไข:** ตรวจสอบว่า Backend API endpoint ถูกต้อง และ server กำลังรันอยู่

### ปัญหา: ฟอร์มไม่แสดง
**แก้ไข:** ตรวจสอบว่า import `PlantDataUploadForm` ใน `App.vue` ถูกต้อง

## 📝 Notes

- ข้อมูลทั้งหมดจะถูกส่งเป็น JSON ไปยัง API
- ฟอร์มจะ reset อัตโนมัติหลังอัพโหลดสำเร็จ 2 วินาที
- สามารถกด "ล้างฟอร์ม" เพื่อล้างข้อมูลทุกฟิลด์
- Loading state จะแสดงขณะกำลังอัพโหลด
- Error จะแสดงด้านล่างฟอร์มถ้ามีปัญหา

---

**สร้างโดย:** GitHub Copilot  
**วันที่:** November 7, 2025
